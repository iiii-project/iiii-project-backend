meta {
  name: Delete Session
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/divinations/{{sessionId}}/
  body: none
  auth: none
}

tests {
  test("deletes the block-cast test session", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.body.success).to.equal(true);
    expect(res.body.data).to.deep.equal({});
  });
}
